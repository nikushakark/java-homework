import ge.edutech.testautomation.javaoop.figures.Circle;

// Press Shift twice to open the Search Everywhere dialog and type `show whitespaces`,
// then press Enter. You can now see whitespace characters in your code.
public class Main {
    public static void main(String[] args) {
        Circle erti  = new Circle(1);
        System. out. println(Circle.numberOfCircleInstances);
        Circle ori = new Circle(2);
        System. out. println(Circle.numberOfCircleInstances);
        Circle sami = new Circle(3);
        System. out. println(Circle.numberOfCircleInstances);
        Circle otxi = new Circle(4);
        System. out. println(Circle.numberOfCircleInstances);
        Circle xuti = new Circle(5);
        System. out. println(Circle.numberOfCircleInstances);

        }
    }